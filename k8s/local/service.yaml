apiVersion: v1
kind: Service
metadata:
  name: app-health-tracker
  labels:
    app: app-health-tracker
spec:
  type: NodePort
  selector:
    app: app-health-tracker
  ports:
    - name: http
      port: 80
      targetPort: 3000
      nodePort: 30080