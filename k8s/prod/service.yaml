apiVersion: v1
kind: Service
metadata:
  name: app-health-tracker
  labels:
    app: app-health-tracker
spec:
  type: LoadBalancer
  selector:
    app: app-health-tracker
  ports:
    - name: http
      port: 80
      targetPort: 3000